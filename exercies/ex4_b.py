from typing import Dict


def count_bits(x: int) -> int:
    # Count the number of 1-bits in an integer.
    return bin(x).count("1")


def count_sales_between_months(
    bitmap_indexes: Dict[int, int], first_month: int, last_month: int,
) -> int:
    # Your code goes here.

    # num_sales: int = ...


    return num_sales
