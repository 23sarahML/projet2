from database import Database
from typing import Dict


def build_bitmap_index_for_days(database: Database) -> Dict[int, int]:
    # Compute a bitmap index for each day of a month.
    # Assume a "worst case" of 31 days per month.

    # Your code goes here.


    return bitmap_indexes
