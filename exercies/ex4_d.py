from typing import Dict
from ex4_b import count_bits


def count_sales_between_dates(
    bitmap_indexes_months: Dict[int, int],
    bitmap_indexes_days: Dict[int, int],
    first_month: int,
    first_day: int,
    last_month: int,
    last_day: int,
) -> int:
    # Count the number of sales between a start date and an end date (inclusive).

    # Your code goes here.

    # num_sales = ...


    return num_sales
