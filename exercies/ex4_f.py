from typing import Dict
from ex4_b import count_bits


def count_sales_between_dates_range_coded(
    bitmap_indexes_months_range_coded: Dict[int, int],
    bitmap_indexes_days_range_coded: Dict[int, int],
    first_month: int,
    first_day: int,
    last_month: int,
    last_day: int,
) -> int:
    # Count the number of sales between a start date and an end date (inclusive).

    # Your code goes here.

    # num_sales = ...


    return num_sales
